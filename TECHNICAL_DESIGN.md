# 小黄笔 Highlight：AI 与后端技术设计

> 版本：1.0 · 2026-08-27

## 1. 结论

默认转写采用阿里云百炼 `qwen-audio-3.0-asr-flash-filetrans`，因为播客产品首先需要准确率、时间戳和说话人分离。`paraformer-v2` 保留为后续经济档。Qwen Audio 质量档约 0.792 元/小时，3 小时约 2.376 元；Paraformer 约 0.288 元/小时，3 小时约 0.864 元。

单个录音文件异步任务只在完成后返回结果，不能直接流式吐出半成品。因此渐进转录的正确实现不是“换成实时模型”，而是后端先按静音点将音频切为约 8–15 分钟的小块，重叠 2 秒，并发转录；完成的连续块立即推送给前端。

官方资料：

- [阿里云百炼模型价格](https://help.aliyun.com/zh/model-studio/model-pricing)
- [语音识别模型选型与限制](https://help.aliyun.com/zh/model-studio/asr-model)
- [Paraformer 录音文件识别 API](https://help.aliyun.com/zh/model-studio/paraformer-recorded-speech-recognition-python-sdk)
- [非实时语音识别任务说明](https://help.aliyun.com/zh/model-studio/non-realtime-speech-recognition-user-guide)

## 2. 哪里需要 AI，哪里不需要

| 环节 | 是否使用 AI | 技术 | 原因 |
|---|---:|---|---|
| 页面标题、简介、音频地址提取 | 否 | DOM/结构化数据解析 | 规则明确，AI 只会增加成本和不确定性 |
| 下载、转码、静音切片、边界拼接 | 否 | ffmpeg + 确定性算法 | 必须可复现，不能让模型改动时间轴 |
| 音频转文字 | 是 | Paraformer-v2 / Qwen Audio ASR | 语音识别模型的核心职责 |
| 说话人分离 | 是，但先由 ASR 完成 | ASR diarization | 先得到 speaker_id，不让 LLM 猜声纹 |
| 跨切片 speaker_id 对齐 | 是 | 声纹 embedding + 相邻轮次 | 每个切片会重新编号，文本模型只能辅助 |
| speaker_id 对应真实姓名 | 是 | 低成本 LLM | 结合节目简介、开场自我介绍、代表发言；必须输出置信度与证据 |
| 错字、专名与断句修正 | 是 | 低成本 LLM，逐块运行 | 只做校对，不改写内容；原始 ASR 永久保留 |
| 片头、广告、正文起点识别 | 是 | 低成本 LLM + 文案规则 | 语义判断比关键词规则稳定 |
| 四类标注（核心观点/方法论/案例分析/数据事实） | 是 | DeepSeek | 默认核心观点、数据事实→黄荧光笔；方法论、案例分析→加粗；案例分析另成整段卡片；只凭内容判断数量，不做关键词密度补足 |
| 章节摘要与节目笔记 | 是 | DeepSeek，Map/Reduce | 长文本先分块，再跨块合并 |
| 时间戳跳转、显示隐藏、上下条 | 否 | 前端状态 | 确定性交互，不需要调用模型 |
| 历史列表、收藏、同步冲突 | 否 | 后端数据库 | 数据产品能力，不应该依赖模型 |

原则：AI 只处理需要语义判断的工作；数据传输、播放、进度、排序、去重、权限和持久化都由普通程序完成。

## 3. 渐进转录流水线

```text
小宇宙公开音频 URL
  → 后端下载到临时文件
  → ffmpeg 转单声道并检测静音
  → 8–15 分钟切片，边界重叠 2 秒
  → 2–4 个 Paraformer 异步任务并发
  → 某块完成：保存 raw_asr + 时间戳 + speaker_id
  → 去掉与前一块重叠的重复句
  → 只发布“从开头连续完成”的块
  → WebSocket/SSE 推送浏览器，正文末尾显示“正在继续转写…”
  → 逐块校对与人名映射结果增量覆盖
  → 全部完成后生成整期章节和摘要
```

前端状态机：

1. `queued`：展示节目与转写准备状态；
2. `transcribing`：已完成的连续段落可以读、复制、跳转；末尾仅显示轻量进行中状态；
3. `polishing`：文字可读，后台逐块补充说话人名字、错字修正和标注；
4. `summarizing`：原文不阻塞，摘要页按章节逐步出现；
5. `completed`：去掉进行中状态，保存最终版本号。

不要把后完成的第 8 块直接插到缺失的第 5 块前面；先在服务端缓存，保证用户看到的时间轴始终连续。

## 4. 三类 AI 后处理任务

### 4.1 说话人命名

输入：节目标题/简介里的主持与嘉宾候选、前 10–20 分钟每个 speaker 的代表句、自我介绍附近文本。

输出：

```json
{
  "speakerId": 1,
  "name": "嘉宾姓名",
  "confidence": 0.91,
  "evidence": ["00:03:12 主持人称呼…", "00:04:01 自我介绍…"]
}
```

低于置信度阈值时继续显示“说话人 1”，绝不强行猜名字。声纹是谁由声纹模型判断；LLM 只负责把已分离的声音标签映射到节目中的候选姓名。

### 4.2 校对

每次只输入一个切片及节目专名词表，输出字符级 patch，而不是整段重写。允许：明显同音错字、专名、数字格式、句末标点和错误断句。禁止：删口头信息、总结、润色观点、改变语气。

数据库同时保存 `raw_text`、`polished_text`、`patches` 和 `model_version`，可一键回滚。

### 4.3 内容理解

完成一个切片即可开始抽取局部标注；整期摘要必须等足够章节材料后再做 Reduce。摘要、标注使用独立任务，互不阻塞。

## 5. 原文标注规则

标注由 `ANNOTATION_EDITOR_SYSTEM` 生成，只返回四类 `type`（`quote` / `method` / `case` / `fact`）的 `notes[]`：

- **核心观点 quote**：说话人明确提出的判断、定义、因果或有解释力的主张；默认黄色荧光笔。
- **方法论 method**：可复用的方法、判断标准、做事路径；默认加粗。
- **案例分析 case**：完整事件/动作链，另成带标题的整段卡片；默认加粗。
- **数据事实 fact**：说明规模、变化、代价、结果或前后比较的数字与事实；默认黄色荧光笔。

特点：

- 不设统一覆盖率，也不做关键词密度补足；覆盖完全交给模型按提示词判断，提示词给出各类型区间（quote 6–16、case 4–8、fact 6–14、method 4–10），宁缺毋滥、不为凑数选低价值句。
- `highlightText` 是逐字稿中 4–400 字的连续精确原文，一字不改；`locatePhrase` 忽略空格定位；定位不到就丢弃。
- 每段开头带 `S` 编号，`segmentIndex` 原样返回；`case` 跨段需拆成多个相邻 case，不能跨段拼接原文。
- case 写 `segment.annotation.type="case"`（整段卡片），quote/fact/method 写 `segment.highlights`（内联 mark）。
- 高亮/加粗的取舍由「高亮/加粗」下拉复选决定，默认：`highlightTypes=["quote","fact"]`、`boldTypes=["method","case"]`；点击同一类型可加可减（`toggleHighlightCategory`），已勾选集合会在重标时按 `regenerateAnnotationsFromDock` 应用。
- 智能分析（自定义方向标注）复用 `type:"companion"` 的着色机制，但独立于共读搭子；共读搭子已于产品中移除。

## 6. 账号、历史与云端数据

后端最小模型：

- `users`：账号；密码使用 scrypt 加盐哈希；
- `sessions`：30 天随机令牌，只存令牌哈希；
- `library`：用户与节目唯一，保存节目元数据、摘要、最终逐字稿、标注结果和个人笔记；
- 后续拆表：`transcription_jobs`、`transcript_chunks`、`annotations`、`notes`。

同步策略：本地优先。每次修改先写 Chrome 本地，网络空闲 1 秒后合并上传。服务端按 `updatedAt` 和对象 ID 合并；个人笔记逐条合并，逐字稿按服务端版本为准。删除使用 tombstone，避免离线设备把已删除内容重新上传。

当前仓库的 `server/` 已提供账号与资料库 API，但扩展端登录默认关闭。只有部署到用户确认的 HTTPS 域名后，才允许发送邮箱、密码、完整原文和私人笔记。

## 7. 推荐上线顺序

1. 用 10 期节目各抽 15 分钟，对比 Paraformer 与 Qwen Audio 的专名、数字、时间戳、说话人切换；
2. 先上线 Paraformer 单块异步 + 渐进前端，再加入说话人命名与校对；
3. 部署历史后端，完成 HTTPS、限流、备份、删除账号/导出数据后再打开登录；
4. 最后上线多设备同步，避免早期把复杂度绑在一条链路上。
