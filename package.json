{
  "name": "highlight",
  "version": "1.0.0",
  "private": true,
  "description": "Highlight and deeply read long-form videos and podcasts.",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check background.js && node --check content.js && node --check settings.js && node --check platform.js && node --check pipeline.js && node --check demo-library.js && node --check sidepanel.js && node --check options.js",
    "debug:annotate": "node tools/annotate-debug.js"
  }
}
